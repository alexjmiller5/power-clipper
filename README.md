# Power Clipper

## Features I'd Like to Add / that might already exist


## Setup

- Clone your fork

```bash
git clone https://github.com/alexjmiller5/power-clipper.git
cd power-clipper
```

- Install dependencies

```bash
npm install
```

- Install the VS Code packaging tool globally

```bash
brew install vsce
```

- Package the extension and install it in VS Code

```bash
vsce package
code --install-extension power-clipper-1.3.0.vsix
```